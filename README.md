```
CLIB - 2025 - ZeleznaRuda

CLIB C++ Core 1.0.0

Help:

    create      - creates a template based on the name ('CMakeLists.txt', 'info.yaml')
    connect     - connect clib to your project
    uninstall   - uninstall library                  (supports the '-f' flag)
    install     - install library                    (supports the '-f' flag)
    init        - initializes clib

    --help      - show this help message and exit
    -h          - show this help message and exit

    --force     - executes the command without question
    -f          - executes the command without question


```
